/*
This is empty on purpose! Your code to build the resume will go here.
 */
 var formattedName = HTMLheaderName.replace("%data%", "Junjie Jin");
// $("#main").append(["Junjie Jin"]);
 $("#header").append(formattedName);